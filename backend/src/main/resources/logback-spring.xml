<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
    <conversionRule conversionWord="clr" class="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex" class="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx" class="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="askbox"/>
    <springProperty scope="context" name="LOG_PATH" source="ASKBOX_LOG_PATH" defaultValue="logs"/>

    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %clr([%X{traceId:-}]){faint} %clr([actorUserId=%X{userId:--}]){faint} %clr([%thread]){magenta} %clr(%-5level) %clr(%logger{36}){cyan} - %msg%n%wEx"/>
    <property name="FILE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{traceId:-}] [actorUserId=%X{userId:--}] [%thread] %-5level %logger{36} - %msg%n%wEx"/>

    <springProfile name="local,dev">
        <springProperty scope="context" name="LOG_ROOT_LEVEL" source="ASKBOX_LOG_ROOT_LEVEL" defaultValue="INFO"/>
        <springProperty scope="context" name="LOG_APP_LEVEL" source="ASKBOX_LOG_APP_LEVEL" defaultValue="DEBUG"/>
        <springProperty scope="context" name="LOG_SQL_LEVEL" source="ASKBOX_LOG_SQL_LEVEL" defaultValue="INFO"/>
    </springProfile>

    <springProfile name="!local &amp; !dev">
        <springProperty scope="context" name="LOG_ROOT_LEVEL" source="ASKBOX_LOG_ROOT_LEVEL" defaultValue="INFO"/>
        <springProperty scope="context" name="LOG_APP_LEVEL" source="ASKBOX_LOG_APP_LEVEL" defaultValue="INFO"/>
        <springProperty scope="context" name="LOG_SQL_LEVEL" source="ASKBOX_LOG_SQL_LEVEL" defaultValue="INFO"/>
    </springProfile>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/${APP_NAME}-error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <logger name="cn.xiuxius.askbox" level="${LOG_APP_LEVEL}"/>
    <logger name="org.mybatis.spring.SqlSessionUtils" level="INFO"/>
    <logger name="org.mybatis" level="${LOG_SQL_LEVEL}"/>
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.apache.catalina" level="INFO"/>
    <logger name="org.apache.tomcat" level="INFO"/>

    <root level="${LOG_ROOT_LEVEL}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>
</configuration>
